{
  "name": "@pittica/logger-helpers",
  "private": false,
  "version": "1.2.0",
  "description": "Logger helpers.",
  "main": "dist/index.js",
  "scripts": {
    "clean": "rimraf ./dist",
    "build": "npm run clean && mkdirp ./dist && babel src --out-dir ./dist --copy-files",
    "test": "echo \"Error: no test specified\" && exit 1",
    "send": "npm run build && run-func ./dist/scripts.js send",
    "email": "npm run build && run-func ./dist/scripts.js sendEmail"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/pittica/logger-helpers.git"
  },
  "keywords": [
    "pittica",
    "logger",
    "slack"
  ],
  "author": "Lucio Benini <lucio.benini@pittica.com>",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/pittica/logger-helpers/issues"
  },
  "homepage": "https://github.com/pittica/logger-helpers#README.md",
  "dependencies": {
    "@google-cloud/logging-winston": "^6.0.2",
    "@slack/web-api": "^8.0.0",
    "date-and-time": "^4.5.1",
    "dotenv": "^17.4.2",
    "nodemailer": "^9.0.3"
  },
  "devDependencies": {
    "@babel/cli": "^7.29.7",
    "mkdirp": "^3.0.1",
    "prettier": "^3.9.6",
    "rimraf": "^6.1.3",
    "run-func": "^3.0.0"
  }
}
